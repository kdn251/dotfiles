# How large the pop-up image should be
max_height=200
max_width=200

# Enable for YouTube and network videos (since you use newsboat/yt-dlp)
network=yes

# Use your GPU for faster seeking
hwdec=yes

# Pre-spawn the background process so thumbnails are instant
spawn_first=yes
