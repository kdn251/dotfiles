[ids]
*

[main]
# Instead of overloading as control, create a custom modifier
capslock = overload(capslock_mod, capslock)  # Hold = custom modifier, Tap = CapsLock

# Define arrow keys for the custom modifier
[capslock_mod]
h = left
j = down
k = up
l = right
