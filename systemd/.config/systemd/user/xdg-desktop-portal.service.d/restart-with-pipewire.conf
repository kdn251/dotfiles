# Restart the portal whenever pipewire restarts (e.g. pipewire gets SIGKILLed
# when the TS4 dock's USB audio vanishes on undock). Otherwise the portal keeps
# a dead PipeWire connection and OBS/screen-share see no ScreenCast sources.
[Unit]
PartOf=pipewire.service
After=pipewire.service
