# xdg-desktop-portal-hyprland config
screencopy {
    # pre-check "allow restore token" in the share picker so apps like OBS
    # get a token and can restore the screen share silently on relaunch
    allow_token_by_default = true
}
